name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v1
      - name: Echo test
        run: echo "hello world $MY_NAME" 
        
        with:
          MY_NAME: "Mona"
